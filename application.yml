apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: myapp-argo-application-helm
  namespace: argocd
spec:
  destination:
    namespace: myapp02
    server: "https://kubernetes.default.svc"
  source:
    path: ''
    repoURL: https://github.com/Patiphannew/helm-Deployment-Intern.git
    targetRevision: "1.2.* or >=1.2.0 <1.3.0"
    chart: helm
  project: default

